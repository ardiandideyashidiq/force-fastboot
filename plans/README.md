# Implementation Plans

Generated by the improve skill on 2026-06-25. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add `cargo test` + `cargo clippy` to CI workflow | P1 | S | — | DONE |
| 002  | Update README CLI docs to match actual CLI | P2 | S | — | DONE |
| 003  | Consolidate `safety_class()` and `role_for_name()` into shared helper | P2 | S | — | DONE |
| 004  | Split `cli/flash.rs` into submodule | P2 | S | — | DONE |
| 005  | Split `plan/mod.rs` into submodules | P2 | M | — | DONE |
| 006  | Refactor `FlashPlanOptions` bools into logical enums | P2 | M | — | DONE (pre-empted by 001) |
| 007  | Extract `WiperConfig` to eliminate 10-parameter `wipe_partition` | P2 | S | — | DONE (pre-empted by 001) |
| 008  | Add integration tests for flash pipeline | P2 | L | 001 | DONE |
| 009  | Harden scatter path resolution: block outside-package_root paths | P3 | M | — | DONE |
| 010  | Deduplicate chunk-sending loop in `flash_sparse_*` functions | P3 | S | — | DONE (pre-empted by 001) |
| 011  | Optimize non-Unix `scan_extents` fallback for large partitions | P3 | L | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 008 requires 001 because CI gates must exist to catch test regressions before adding new tests.
- Plans 001–007 are independent of each other.
- 009–011 are lower-priority independent plans.

## Findings considered and rejected

- `transition_mode` retry logic (`gsi/flash.rs:146-166`): confusing but correct; not worth changing.
- `clear_bootloader_bcb` early return: correct behavior per comment.
- `image_magic` truncation protection: safe due to `.get()`.
- Non-Unix `scan_extents` fallback known limitation: Linux-primary tool; lowest priority.
- `GsiCounters` → `GsiFlashSummary` `usize` conversion on 32-bit platforms: negligible odds.
