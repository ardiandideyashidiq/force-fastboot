# Implementation Plans

Generated by the improve skill on 2026-06-25. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Commit referenced: `12bf062`

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix `sparse_wrap_file` truncation destroying filesystem data | P1 | S | — | DONE |
| 002 | Set `package_root` and block path traversal in scatter image resolution | P1 | S | — | DONE |
| 003 | Add USB device selection confirmation before destructive operations | P1 | S | — | DONE |
| 004 | Add user confirmation before sudo privilege escalation | P1 | S | — | DONE |
| 005 | Add characterization tests for flash execution core | P1 | L | — | DONE |
| 006 | Fix silent sentinel fallback in i64→u64 for `lseek` results | P2 | S | — | DONE |
| 007 | Consolidate duplicated download→send→flash cycle into shared helper | P2 | L | 005 | TODO |
| 008 | Fix `tracing` format strings to use structured fields | P2 | S | — | DONE |
| 009 | Simplify GSI dual-path state machine | P2 | S | 005 | TODO |
| 010 | Update deprecated/abandoned dependencies (`colored`→`owo-colors`) | P3 | M | — | DONE |
| 011 | Wire up orphaned `cancel_token` in GSI flash | P3 | S | — | DONE |
| 012 | Add `--json` output for flash results | P3 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 007 (download→send→flash refactor) requires 005 (characterization tests) because the refactor touches the protocol core and needs a safety net.
- 009 (GSI state machine) requires 005 for the same reason — the GSI flash uses the same flash internals.

## Findings considered and rejected

- "BCB clear partial write" — best-effort by design, each step warns on failure.
- "USB re-enumeration in preloader loop" — correct by design, must detect fastboot mode immediately.
- "Progress bar abandoned on flash failure" — executor exits loop after all partitions; no behavioral impact.
- "Retry double-reboot in GSI transition" — harmless second reboot, wastes ~120s on rare edge case.

## Build / test / lint commands (all plans)

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| Build | `cargo build` | exit 0 |
| Release build | `cargo build --release` | exit 0 |
| Test | `cargo test` | all pass |
| Lint | `cargo clippy --all-targets --all-features --locked -- -D warnings` | no warnings |
